# Test of MetaModelica options.
# Function returns asin(x) if defined, None otherwise.


from math import asin

def asin0028(x):
    if x > 1.0 or x < -1.0:
        return
        # Alternative syntax:
        # return None
    else:
        return asin(r)
