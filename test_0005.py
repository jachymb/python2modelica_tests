# Simple test for if statement. 
# Returns the absolute value of a 'float'.
# The code is intentionaly redundant to use
# the 'elif' branch.

def abs0005(r):
    if r > 0:
        return r
    elif r < 0:
        return -r
    else
        return 0
